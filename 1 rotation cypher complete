#include <stdio.h> 
#include <string.h>

int main() {

int rot; // 'rot' for rotation
printf("enter the rotation amount:\n");
scanf("%d", &rot);

int i = 0
const char inputtext[i];
printf("enter the input text\n");
scanf("%s", inputtext);
 
int length;
length = strlen(n);

if(i == 0; i <= n; i++){
    inputtext[i] = inputtext[i] - rot; //will work for any rotation < 26
    printf("%c", inputtext[i]);
}

return 0;
}
