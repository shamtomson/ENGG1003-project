#include <stdio.h> 
#include <string.h>

int main() {

int rot; // 'rot' for rotation
printf("enter the rotation amount:\n");
scanf("%d", &rot);

const char inputtext[1024];
printf("enter the input text\n");
scanf("%s", inputtext);

const char inputtext[1024];
 scanf("%s", inputtext);
 
 int length;
 length = strlen(inputtext);
 
 
 //length can then be used to give each part of array its location
 
 
 
int let[1024]; //let for individual letters




return 0;
}
