#include <stdio.h>
#include <string.h>
 
int main()
{
 const char inputtext[1024];
 printf("enter the input text\n");
 scanf("%s", inputtext);
 
 int length;
 length = strlen(inputtext);
 printf("length = %d\n", length);
 
 return 0;
}
